# mee_yatt_htar

** Local Sync employee info through bridge of python local server **
- Employee info (CRUD)
- python flask server to sync data with the trigger button on android
    - compare two database changes(sqlite<android>, mysql<windows>)
    - mysql first sync ( two db changes will cover with mysql changes )
